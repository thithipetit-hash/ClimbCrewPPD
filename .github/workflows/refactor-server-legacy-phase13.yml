name: Refactor server legacy phase 13

on:
  push:
    branches:
      - refactor/server-legacy-phase13-20260823
    paths:
      - backend/tools/refactor-server-legacy-phase13.mjs

permissions:
  contents: write

jobs:
  refactor:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: refactor/server-legacy-phase13-20260823
          fetch-depth: 0
      - name: Apply deterministic refactor
        run: node backend/tools/refactor-server-legacy-phase13.mjs
      - name: Validate generated source
        run: |
          node --check backend/server.js
          node --check backend/default-admin-bootstrap.js
          git diff --check
      - name: Commit generated server.js
        run: |
          if git diff --quiet -- backend/server.js; then
            echo "No generated change"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend/server.js
          git commit -m "Sortir le bootstrap administrateur de server.js"
          git push origin HEAD:refactor/server-legacy-phase13-20260823
